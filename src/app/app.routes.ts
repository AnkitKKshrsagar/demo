import { Routes } from '@angular/router';
import { MonaliComponent } from './monali/monali.component';

export const routes: Routes = [
    {
        path:'monali',component :MonaliComponent
    }
];
